# Clash配置解析器环境配置

# 运行环境: dev|test|prod
ENVIRONMENT=dev

# 服务器端口
PORT=8080

# 日志级别: debug|info|warning|error
LOG_LEVEL=info

# Uvicorn工作进程数 (生产环境建议设置为CPU核心数)
UVICORN_WORKERS=1

# Redis配置 (可选，如果不使用Redis可以不配置)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=
# REDIS_TIMEOUT=30